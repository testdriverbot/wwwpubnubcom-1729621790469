version: 4.0.70
steps:
  - prompt: Click on "Try for free".
    commands:
      - command: hover-text
        text: Try for free
        description: button in the header
        action: click
  - prompt: >-
      Assert that the user lands on the sign-up or trial page.  # Use the
      content of the file from the environment
    commands:
      - command: assert
        expect: the user lands on the sign-up or trial page
